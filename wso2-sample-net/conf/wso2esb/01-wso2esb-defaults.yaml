---

# eleks: for reviev. worker / manager
product_profile: 'manager'

# esb_wsdl_epr_prefix.http.bind_address
# esb_wsdl_epr_prefix['http'].bind_address
# esb_wsdl_epr_prefix['http']['bind_address']
esb_wsdl_epr_prefix:
  http:
    prefix: http://${server.esb.public.host}:${server.esb.public.port.http}/todo
    bind_address: 0.0.0.0
  https:
    prefix: https://${server.esb.public.host}:${server.esb.public.port.https}/todo
    bind_address: 0.0.0.0

  
ports:
  proxyPort:
     pass_through_http: some param

hostname: some param
mgt_hostname: some param
