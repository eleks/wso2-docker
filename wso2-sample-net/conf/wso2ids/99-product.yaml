---
##### CUSTOM PRODUCT CONFIG
# override  the default values here

entitlement:
  "PDP.Global.Policy.Combining.Algorithm": "urn:oasis:names:tc:xacml:3.0:policy-combining-algorithm:deny-unless-permit"

user_management:
  UsernameJavaRegEx: '[a-zA-Z0-9.\-_-|//]{3,30}$'

identity_analytics:
  enabled: true
  username: admin
  password: admin
